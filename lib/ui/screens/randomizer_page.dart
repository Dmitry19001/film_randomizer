import 'package:flutter/material.dart';

class RandomizeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Randomize Screen"),
      ),
      body: Center(
        child: Text("This is the Randomize Screen"),
      ),
    );
  }
}
